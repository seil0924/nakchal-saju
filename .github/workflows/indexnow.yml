name: IndexNow
on: {push: {branches: [main]}, workflow_dispatch: {}}
jobs: {ping: {runs-on: ubuntu-latest, steps: [{uses: actions/checkout@v4}, {name: Submit sitemap to IndexNow, run: bash scripts/indexnow.sh}]}}
